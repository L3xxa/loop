
# Generated by Qodo Gen

import unittest
from main import simple_numbers
class TestSimpleNumbers(unittest.TestCase):

    # Generates prime numbers for a valid range of numbers
    def test_generate_prime_numbers_valid_range(self):
        from io import StringIO
        import sys
        captured_output = StringIO()
        sys.stdout = captured_output
        simple_numbers(10, 20)
        sys.stdout = sys.__stdout__
        self.assertEqual(captured_output.getvalue().strip(), '[11, 13, 17, 19]')

    # Handles negative numbers as input for the range
    def test_handle_negative_numbers(self):
        from io import StringIO
        import sys
        captured_output = StringIO()
        sys.stdout = captured_output
        simple_numbers(-10, 2)
        sys.stdout = sys.__stdout__
        self.assertEqual(captured_output.getvalue().strip(), '[2]')

    # Manages cases where the start and end numbers are the same
    def test_same_start_end_number(self):
        from io import StringIO
        import sys
        captured_output = StringIO()
        sys.stdout = captured_output
        simple_numbers(5, 5)
        sys.stdout = sys.__stdout__
        self.assertEqual(captured_output.getvalue().strip(), '[5]')
